services:
  movilab-portada:
    build: .
    container_name: movilab-portada
    restart: unless-stopped
    env_file: .env
    volumes:
      - movilab_data:/app/data
    networks:
      - proxy_network

volumes:
  movilab_data:

networks:
  proxy_network:
    external: true
